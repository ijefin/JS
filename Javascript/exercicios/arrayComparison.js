function compareTriplets(a, b) {
  let maxValue = 2;

  for (let i = 0; i <= maxValue; i++) {
    const arrayA = [5, 8, 1];
    const arrayB = [5, 6, 7];

    const teste = [arrayA[i] > arrayB[i]];

    teste.filter((item) => item.true);
    console.log(teste);
  }
}

compareTriplets();

//Compare if the value is bigger than or equals.
